#we must pass 2 variable component and environment
- name: "configure {{ component }} server"
  hosts: all
  become: yes
  vars_files:
  - "variable-{{ environment }}.yaml"
  roles:
  - "{{ component }}"

